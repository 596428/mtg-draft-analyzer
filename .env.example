# MTG Draft Analyzer - Environment Variables

# Gemini API Key (Required for LLM analysis)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Use secondary API key for rotation
GEMINI_API_KEY_2=

# Cache directory (optional, defaults to .cache)
CACHE_DIR=.cache

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Output directory (optional, defaults to output)
OUTPUT_DIR=output
